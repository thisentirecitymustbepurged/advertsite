<Row className="detailed-segments">
          <Grid>
            <Col xs={7} md={7} className="segments-table sans-left-pad">
              <SegmentsTable selectedSegments={segments} segments={audience.segments} followers={audience.followers} regionDesc={region.segmentDesc} onClick={this.handleAddSegment} />
            </Col>
            <Col xs={5} md={5}>
              <ChartManager data={this.props.store.ui.audience.summary} config={config} analytics={app.analytics} onClick={this.handleAddSegment} />
            </Col>
          </Grid>
        </Row>
        <Row id="segment-tabs-container">
          <Tabs defaultActiveKey={1} id="segment-tabs">
            <Tab eventKey={1} title="Stars">
              <Grid>
                <SegmentFilters audience={this.audience} />
                <StarsTableContainer audience={this.audience} segments={segments} modal={this.modal} analytics={app.analytics} />
              </Grid>
            </Tab>
            <Tab eventKey={2} title="Passions">
              <Grid>
                <PassionsTableContainer audience={this.audience} segments={segments} passions={importantPassions} modal={this.modal} />
              </Grid>
            </Tab>
            {/*
            <Tab eventKey={3} title="Vibe" disabled>
              Content for Vibe
            </Tab>
            */}
          </Tabs>
        </Row>
import { Grid, Row, Col, Tabs, Tab } from 'react-bootstrap';